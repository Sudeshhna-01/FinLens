DATABASE_URL="postgresql://user:password@localhost:5432/finlens?schema=public"
JWT_SECRET="your-super-secret-jwt-key"
PORT=5000

# Optional: real-time / delayed stock quotes (https://finnhub.io/register)
# Used by /api/stocks/market-data and syncs Stock.currentPrice; copy the same key into ml/.env for ML scripts.
FINNHUB_API_KEY=""
